<Window x:Class="WindowsPerformanceMonitor.ScheduleLogProcess"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsPerformanceMonitor"
        mc:Ignorable="d"
        Closing="ScheduleLogView_Closing"
        Title="ScheduleLogProcess" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Left (View type, graph type, process to graph)-->
        <StackPanel
            Grid.Row="0" 
            Margin="20,10,20,10"
            >
            <Label Content="Enter Time (hrs:mins AM/PM):" FontWeight="Bold"/>
            <TextBox  x:Name="TimeTextBox" Width="180" HorizontalAlignment="Left"/>

            <Label Content="Enter Duration (hrs:mins):" FontWeight="Bold"/>
            <TextBox  x:Name="DurationTextBox" Width="180" HorizontalAlignment="Left"/>
        </StackPanel>

        <Button Grid.Row="1" Margin="20, 3, 20, 5" Click="SaveScheduleLog_Click">Schedule Log</Button>

    </Grid>
</Window>
